# Linux commands

If you are in Ubuntu and want to create a shortcut to a directory you can use:
```bash
 ln -s /mnt/c/XX/XXX/ ~/exercises
```
Make sure you are in the home directory and then:
```bash
cd ~/exercises
```
